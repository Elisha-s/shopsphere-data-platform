# This is a Declarative Automation Bundle definition for shopsphere_data_platform.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: shopsphere_data_platform
  uuid: cb64147f-9f8a-41d3-913e-8e148c1375ba

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616524873712.12.azuredatabricks.net
    variables:
      catalog: adb_shopsphere_dev
      schema: bronze
  prod:
    mode: production
    workspace:
      host: https://adb-7405616524873712.12.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/elisha.singh@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/elisha.singh@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: adb_shopsphere_dev
      schema: prod
    permissions:
      - user_name: elisha.singh@outlook.com
        level: CAN_MANAGE
